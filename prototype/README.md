# Visual prototype

This is a standalone HTML prototype for defining the Pelota de Medias experience. The home page is a chronological feed of compact one-line match, news, and event entries. Its data is fictional and lives only in `app.js`; it makes no network requests and has no automatic collection or refresh.

Open `index.html` directly, or serve this folder with any static web server.
